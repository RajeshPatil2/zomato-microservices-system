package com.zomato.order.kafka;

/*
 * @Service public class OrderProducer {
 * 
 * private static final String TOPIC = "order-placed";
 * 
 * @Autowired private KafkaTemplate<String, OrderEvent> kafkaTemplate;
 * 
 * public void sendOrderEvent(OrderEvent event) { kafkaTemplate.send(TOPIC,
 * event); System.out.println("Order event sent to Kafka"); } }
 */